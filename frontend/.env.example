# Exemple de fichier .env pour le frontend
# Copier ce fichier en .env et remplir les valeurs si nécessaire

# API Backend URL (utilisé en production)
# En développement, le proxy Vite gère automatiquement
VITE_API_URL=http://localhost:8000/api/v1
# Pour un accès depuis le réseau local, remplacez localhost par l'adresse IP de la machine qui héberge le backend, par ex.:
# VITE_API_URL=http://192.168.1.X:8000/api/v1
# Si cette variable est absente, le frontend utilisera automatiquement l'origine courante + /api/v1
